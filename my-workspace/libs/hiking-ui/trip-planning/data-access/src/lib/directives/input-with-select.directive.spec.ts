import { InputWithSelectDirective } from './input-with-select.directive';

describe('InputWithSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new InputWithSelectDirective();
    expect(directive).toBeTruthy();
  });
});
